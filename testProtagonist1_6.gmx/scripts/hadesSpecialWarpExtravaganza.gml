/// hadesSpecialWarpExtravaganza()
// has nothing to do with Hades, but does have things to do with magical spirals

obj_map.X = 2;
obj_map.Y = 0;
createRoomTransition(obj_map.map[# obj_map.X, obj_map.Y], obj_fadeTransition);

