///unhideDrawer(drawer)
// every drawer should have a hide sequence that initiates when drawer.hide == true
// the hide sequence should be canceled when drawer.hide == false
// This method unhides the drawer by setting hide to false.
var drawer = argument0;
drawer.hide = false;
