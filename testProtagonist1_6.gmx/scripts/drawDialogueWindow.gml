///drawDialogueWindow(startX, startY, border, width, height)
var startX = argument0;
var startY = argument1;
var border = argument2;
var width = argument3;
var height = argument4;

// draw back window
drawDialogueRect(startX, startY, width, height, border);
