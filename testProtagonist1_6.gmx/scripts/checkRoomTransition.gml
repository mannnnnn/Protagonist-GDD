///checkRoomTransition()
// checks to see if we are transitioning to another room. true == we are transitioning.
// for example, no interactions should occur in such a case.

return instance_number(obj_roomTransition) > 0;
