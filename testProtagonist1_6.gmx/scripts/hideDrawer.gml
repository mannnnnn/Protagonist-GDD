///hideDrawer(drawer)
// every drawer should have a hide sequence that initiates when drawer.hide == true
// the hide sequence should eventually lead to drawer being not visible
// This method starts the drawers's hide sequence by setting hide to true.
var drawer = argument0;
drawer.hide = true;
